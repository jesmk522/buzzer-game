# Copy this to .env (never commit .env)

# Server
PORT=4120

# Evidence Pack HMAC signing secret — MUST be set in prod
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
EVIDENCE_SECRET=

# (Phase 1 問答) AdSense — Ace 提供
ADSENSE_PUBLISHER_ID=

# (Phase 2) 題庫 / 章節 DB（暫未使用）
# DATABASE_URL=
